#include <iostream>
using namespace std;

int main()
{
    /*
        arr = [2 | 1 | 3 | 4]
        N = 4 (number of books)
        M = 2 (number of students)

        st = 0, end = sum(arr)

        while(st <= end)
        {
            mid = st + (end - st) / 2;

            if(isvalid(mid)) // valid
            {
                end = mid - 1; // move left
                ans = mid; 
            }
            else // invalid
            {
                st = mid + 1; // move right
            }
        }
    */
    return 0;
}