#include <iostream>
using namespace std;

int main()
{
    /*
        start = 0, end = n-1

        while (start <= end)
        {
            mid = start + (end - start) / 2;

            if(A[mid-1] < A[mid] < A[mid+1])
            {
                return mid;
            }
            else if(A[mid-1] < A[mid]) // move right
            {
                start = mid + 1;
            }
            else // move left
            {
                end = mid - 1;
            }
        }
    */
    return 0;
}