#include <iostream>
using namespace std;

int main()
{
    // Binary Search:

    /*
        Real-life example:
        In dictionaries,
        by opening a random page at starting,
        based on the first letter of the word,
        we then move left or right of the page.

        If we wnat to search "parrot",
        open the page with word "dog",
        P is after D,
        so we move to the right side of the page.

        The data in dictionary is sorted,
        A, B, C..... X, Y, Z
    */

    return 0;
}