#include <iostream>
using namespace std;

int main()
{
    /*
        Recurrence Relation:

        It is a mathematical way of representing our recursion,
        which is later used to calculate the time complexity.

        e.g. for our printNums function:

        Recurence Relation:
        f(n) = print"n" + f(n-1)

        Time Complexity (using recurrence relation):
        TC(n) = O(1) + TC(n-1)

    */
    return 0;
}